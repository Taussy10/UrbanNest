 Always add ScrollView for each screen if user devices small then they can scroll 
  {/* use redirect while working on any screen 
    app will refresh and move to that screen*/}
    {/* <Redirect href={'/details'} /> */}

    import { StyleSheet, Text, View , Image, TouchableOpacity, ScrollView } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
import React from 'react'
import { Redirect } from 'expo-router'
import images from '~/constants/images'
import icons from '~/constants/icons'
import { LinearGradient } from 'expo-linear-gradient';
import { useRouter } from 'expo-router'

const Onboarding = () => {
  const router = useRouter()

  const move = () => {
    router.replace('/(tabs)/home')
  }
  return (
    <SafeAreaView
    className='flex-1  bg-[#f4f4f4]   '>
     {/* use redirect while working on any screen 
    app will refresh and move to that screen*/}
    {/* <Redirect href={'/details'} /> */}

{/* Always add ScrollView for each screen if user devices small then they can scroll */}
<ScrollView

// this make content is full on the screen learn about it 
contentContainerClassName= 'h-full'
>

{/* so this screen has 2 mainGroups
1.Image and texts 
*/}
{/* Image mainGroups */}
<View>
<Image 
      source={images.onboarding}
      className= 'h-[60%] w-full'
      />
</View>



<LinearGradient 
 colors={['#ffffff', 'transparent']}

>

      <View
      className=' flex-col items-center '
      >
{/* look what are the props he uses for text */}
      <Text className=' text-base mb-2 text-center uppercase font-rubik text-black-200'>WELCOME TO REAL SCOUT</Text>

      {/* Cause it's group of welcoming text */}
      <View
      className=' flex-col mb-4'
      >
      <Text className=' text-2xl  text-center  font-rubik-bold'>Let's Get You Closer {"\n"}</Text>
      {/* <Text className=' text-2xl font-bold'>TO  */}
        <Text
      className=' text-blue-500 text-2xl '>Your Ideal Home </Text>
      {/* </Text> */}
      </View>
 
      <Text className='text-[#666876]'>Login to Real Scout with Google</Text>

      <TouchableOpacity
      activeOpacity={0.7}
      className= '  bg-white h-12  w-[85%]  rounded-2xl  justify-center items-center'
      onPress={move}
      >

        <View
        className=' flex-row justify-center items-center gap-3'
        >

          <Image source={icons.google}
          className=' size-7'
          />
        <Text>Sign Up with Google</Text>

        </View>
      </TouchableOpacity>

      </View>

      </LinearGradient>
      </ScrollView>

    </SafeAreaView>
  )
}

export default Onboarding

const styles = StyleSheet.create({

})

